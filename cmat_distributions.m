 %%% MIN MEAN MAX HISTOGRAMS for CMAT

% mins = zeros(1, 308);
% means = zeros(1, 308);
% maxs = zeros(1, 308);

% for x = 1:308;
%     mins(x) = min(min(cmat(:, :, x)));
%     means(x) = mean(mean(cmat(:, :, x)));
%     maxs(x) = max(max(cmat(:, :, x)));
% end
